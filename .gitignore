# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv

# Model artifacts (except backend/diabetes_ensemble.pkl for Vercel serverless)
*.pkl
!backend/diabetes_ensemble.pkl
*.joblib

# Dataset cache (if downloaded locally instead of fetched at runtime)
*.csv
!sample_data/*.csv

# Flask
instance/
.flaskenv

# Environment variables / secrets — NEVER commit API keys
.env
.env.local
*.key

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Node (in case any tooling is added later)
node_modules/

# Logs
*.log
